#cloud-config
autoinstall:
  version: 1
  locale: en_US.UTF-8
  identity:
    hostname: pvmlab-target
    # The password is "ubuntu"
    password: "$6$ex/yH.Med64."
    username: ubuntu
  ssh:
    install-server: true
    allow-pw: true
  storage:
    layout:
      name: direct
  packages:
    - qemu-guest-agent
