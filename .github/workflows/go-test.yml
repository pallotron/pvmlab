# This workflow runs tests for the Go project.
#
# It is triggered on push events to the main branch and on pull requests
# targeting the main branch.
#
# It uses the standard Go test runner to execute all tests in the project.

name: Go Test

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: "./go.mod" # Or use a .go-version file
          cache: true

      - name: Install QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 ovmf qemu-efi-aarch64

      - name: Test
        run: |
          export RUN_INTEGRATION_TESTS=false
          go test ./... -v -race -coverprofile=coverage.txt

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: unittests
          verbose: true
